[build]
target-dir = "target"

[alias]
bin = ["run", "--package", "cargo-bin", "--"]

# Full workspace validation (slow; use only for pre-push/CI)
build-all = ["build", "--workspace", "--all-targets", "--all-features"]
clippy-all = ["clippy", "--workspace", "--all-targets", "--all-features", "--", "-D", "warnings"]
fmt-check = ["fmt", "--all", "--", "--check"]
test-all = ["test", "--workspace", "--all-targets", "--all-features"]

# Targeted validation (recommended for most development)
build-pkg = ["build", "-p", "--all-targets", "--all-features"]
clippy-pkg = ["clippy", "-p", "--all-targets", "--all-features", "--", "-D", "warnings"]
test-pkg = ["test", "-p", "--all-targets", "--all-features"]
